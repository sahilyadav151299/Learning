extends layouts/main-layout.pug

block styles 
    link(rel="stylesheet", href="/css/main.css")

block content 
    main   
        if prods.length > 0
            h1 My Products
            p List of all added Products 
            
            each product in prods 
                p Product Name : #{product.title} 

        else
            h1 No Products
    

